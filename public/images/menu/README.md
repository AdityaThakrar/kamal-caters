# Menu images

Drop menu images here.

All images must be in **`.avif`** format and sized to **352px width × 528px height** to maintain consistent aspect ratios across cards and modals.

Use paths under `public/`, referenced in `src/data/menuData.ts` as e.g. `/images/menu/your-file-name.avif`.
